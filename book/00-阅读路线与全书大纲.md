# Claude Code 源码精读教程总纲

> 目标：把 reference/claude-code 这个 Bun + TypeScript + React/Ink 的 CLI Agent 工程，拆成一套可以循序渐进阅读的源码精读教程。

## 一、先给结论：这个工程到底在做什么？

如果只用一句话概括，这个项目是在终端里实现一个可交互、可调用工具、可维护会话状态、可扩展技能与 MCP 能力的 AI Coding Agent。

它的主流程不是“用户问一句，模型答一句”，而是：

1. 从 CLI 启动，做最小化 fast-path 分流。
2. 进入 main.tsx，解析命令行参数，完成初始化、鉴权、配置加载和命令装配。
3. 根据运行模式进入 REPL、print 模式、daemon、bridge、MCP server 等不同分支。
4. 在 REPL 或 SDK 会话里构造 QueryEngine。
5. QueryEngine 在每轮用户输入时拼装 system prompt、user context、tools、commands、MCP clients。
6. query.ts 中的智能体循环负责发请求、接流式响应、提取 tool_use、执行工具、把 tool_result 喂回模型，直到本轮任务结束。
7. 整个过程中，权限系统、上下文系统、记忆系统、命令系统、插件系统和特性开关系统一起工作。

所以，这不是一个单纯的“聊天 CLI”，而是一个完整的“终端 Agent 操作系统”。

## 二、术语约定

为了避免后面各章对同一概念使用不同叫法，这里先统一几组核心术语：

1. 统一运行时：指 Claude Code 这整套会话、工具、权限、任务、远程接入共同组成的总体执行模型。
2. 子代理运行时：指以 `runAgent` 为核心、由子代理独立继承上下文并执行的一层二级运行时。
3. 统一任务框架：也就是后文有时写到的“任务体系”或“任务状态机”，核心都指 `Task.ts + utils/task/framework.ts + AppState.tasks` 这套统一任务抽象。
4. 权限仲裁链：统一指后文出现的 `permission race`、`first-wins arbitration`、`多来源仲裁系统` 这一类概念，也就是多个决策来源共同竞争同一权限结果的链路。
5. 统一控制面：统一指 bridge、remote session、direct connect、ssh 等不同 transport 在 REPL 顶层被收敛后的那层会话控制接口。
6. teammate：当源码名、类型名或 UI 语义直接使用 `teammate` 时，正文保留英文；在解释性文字里可理解为“协作节点”。

## 三、推荐阅读顺序

建议按下面的顺序看源码，不要一上来就钻 query.ts。

1. 先看入口：entrypoints/cli.tsx
2. 再看主装配：main.tsx
3. 再看初始化与渲染：setup.ts、interactiveHelpers.tsx、replLauncher.tsx
4. 再看上下文：context.ts，以及 system prompt 相关文档
5. 再看会话编排：QueryEngine.ts
6. 最后再看智能体循环核心：query.ts
7. 然后反过来补工具、权限、命令、技能、MCP、记忆等外围系统

这个顺序的原因很简单：

- main.tsx 决定“整个程序怎么进入工作状态”。
- QueryEngine 决定“单个会话如何编排”。
- query.ts 决定“单轮任务如何循环执行”。
- tools.ts、commands.ts、context.ts 则分别提供“能力池”“指令池”“上下文池”。

### 读者路线建议

如果你不是准备逐章精读，而是想按目标挑重点，可以直接走下面三条路线。

1. 快速通关路线：先读第 1-6 章，再补第 10、12、20 章。适合先建立“入口装配 -> 会话编排 -> 智能体循环 -> REPL 交互 -> API 通信 -> 统一控制面”的总图。
2. 深水区路线：在快速通关之后，继续读第 7、11、16、17、18、19 章。它们分别对应工具系统、并发执行、MCP 接入、任务框架、权限回调链和 swarm 协作，是整套运行时最容易拉开理解层次的部分。
3. 工程化专题路线：按第 9、13、14、15 章阅读。它们更适合关心长期记忆、会话恢复、执行现场切换和子代理体系的人。

如果只想知道 Claude Code 为什么复杂，第 1-6 章已经足够。如果想知道它为什么“工程上站得住”，第 7、9、11、17、18、19 章是最值得反复回看的章节。

## 四、全书章节设计

### 第 1 章：全局架构与启动流程

重点回答：

- 这个项目的主模块分层是什么？
- CLI 从哪里启动？
- 为什么入口分成 cli.tsx 和 main.tsx 两层？
- fast-path 和完整初始化路径分别解决什么问题？

### 第 2 章：CLI 入口与 Commander 命令装配

重点回答：

- main.tsx 为什么这么大？
- Commander 在这里不是“命令定义文件”，而是整个运行时装配中心，这一点怎么理解？
- preAction 做了哪些真正关键的初始化？
- 默认 action 是如何把“交互模式、print 模式、resume、remote、ssh、assistant”等路线统一起来的？

### 第 3 章：环境初始化与首次渲染

重点回答：

- setup.ts 负责什么，为什么它不只是“初始化脚本”？
- 工作目录、worktree、tmux、hooks watcher、session memory 是怎么在启动时挂起来的？
- REPL 为什么通过 replLauncher.tsx 和 renderAndRun 两层渲染？
- 首次渲染之后又为什么要延迟启动一批 prefetch？

### 第 4 章：上下文构建与 System Prompt

重点回答：

- context.ts 的 system context 和 user context 分别是什么？
- git status、CLAUDE.md、日期为什么要分开构造？
- 为什么 system prompt 要做缓存友好的动态拼装？
- 项目记忆、CLAUDE.md 和普通消息上下文的职责边界是什么？

### 第 5 章：QueryEngine 会话编排

重点回答：

- 为什么项目里同时有 QueryEngine.ts 和 query.ts？
- QueryEngine 到底保存了哪些跨轮状态？
- submitMessage 做了哪些前置编排，才把任务送进 query.ts？
- 为什么说 QueryEngine 是“会话级 orchestrator”，而不是“模型调用器”？

### 第 6 章：query.ts 智能体循环

重点回答：

- query.ts 的 while(true) 循环每轮做什么？
- tool_use、tool_result、streaming、fallback、compact、stop hook 是如何串起来的？
- 为什么这个循环才是 Claude Code 的“智能体内核”？
- 它如何处理超长上下文、输出截断、模型切换和中断恢复？

### 第 7 章：工具系统与权限控制

重点回答：

- getAllBaseTools、getTools、assembleToolPool 分别处在工具装配链的哪一层？
- 为什么工具系统里有 feature flag、平台判断、权限过滤、REPL/simple mode 裁剪和 deny rule 前置过滤多层边界？
- partitionToolCalls、runTools、StreamingToolExecutor 如何把“可见工具”变成“可安全执行的工具调用”？
- 权限系统为什么要同时影响工具可见性、调用审批和失败反馈，而不是只做 UI 确认？

### 第 8 章：命令系统、技能与 MCP 扩展

重点回答：

- COMMANDS、getSkills、loadAllCommands、getCommands 这条命令装载链是怎么分层的？
- dynamic skills 为什么不直接塞进冷启动缓存，而要单独插层？
- getSkillToolCommands、getSlashCommandToolSkills、getMcpSkillCommands 分别服务谁？
- 为什么命令系统其实是“面向人、面向模型、面向远程桥接场景”的多视角能力索引？

### 第 9 章：记忆、会话恢复与工程化设计

重点回答：

- memory files、CLAUDE.md、memdir 自动记忆机制分别通过哪条链路进入会话？
- history.ts、sessionStorage.ts、conversationRecovery.ts、sessionRestore.ts 各自负责哪一种“持久化”？
- Session 持久化和 resume 为什么不是 QueryEngine 自己做，而是单独拆成恢复工程层？
- worktree、todo、file history、context collapse 这些副状态是怎样跟着 resume 一起被恢复的？

### 第 10 章：REPL 交互层与输入提交流程

重点回答：

- REPL.tsx 为什么会这么大？
- 用户输入是如何从 PromptInput 一路流到 onSubmit、handlePromptSubmit、processUserInput 和 onQuery 的？
- REPL 如何把 commands、tools、MCP、notifications、dialogs 聚合成一份“当前交互世界”？
- QueryGuard 的 reserve、tryStart、end、forceEnd 为什么需要三态机而不是布尔 loading？
- queue、interrupt、immediate local-jsx command 这些交互策略为什么必须停留在 UI 桥接层？

### 第 11 章：工具执行器与并发控制

重点回答：

- partitionToolCalls、runTools、runToolsConcurrently、runToolsSerially 之间是怎样分工的？
- 工具并发安全为什么既取决于工具类型，也取决于具体输入？
- 为什么并发执行后的 contextModifier 必须按原顺序回放，而不能随完成顺序落地？
- StreamingToolExecutor 的状态机、sibling abort、synthetic error 和 discard 是怎么共同维持轨迹闭合的？

### 第 12 章：API 通信、Prompt Cache 与上下文压缩

重点回答：

- toolToAPISchema、splitSysPromptPrefix、buildSystemPromptBlocks 是如何把提示词和工具 schema 变成稳定 API 请求的？
- 为什么 MCP tools、provider 差异和 beta shape 会直接影响 cache scope 与请求结构？
- getEffectiveContextWindowSize、calculateTokenWarningState、shouldAutoCompact、autoCompactIfNeeded 是怎样组成预算治理链的？
- Claude Code 是如何在“上下文窗口、缓存命中、协议兼容、恢复能力”之间做平衡的？

### 第 13 章：Continue、Resume 与 Teleport 恢复专题

重点回答：

- continue、resume、teleport 这三条恢复路径共用了哪些底层基础设施？
- deserializeMessagesWithInterruptDetection、processResumedConversation 分别解决恢复链的哪一层问题？
- forkSession、resume picker、PR 过滤、custom title 匹配各自改变了哪一段恢复流程？
- 为什么 teleport 在恢复前必须先解决仓库映射、分支校验和远端日志翻译？

### 第 14 章：Worktree 隔离与 Remote 运行模式

重点回答：

- WorktreeSession、getOrCreateWorktree、restoreWorktreeForResume 是如何把“隔离工作区”变成“会话现场”的？
- direct connect、ssh、remote、assistant viewer 这几种运行模式各自改变了哪一层执行拓扑？
- RemoteSessionManager 为什么不仅是 WebSocket 客户端，还是权限桥接和会话控制器？
- 为什么命令、工具、cwd、prompt cache 在这些模式切换下都需要重新理解？

### 第 15 章：AgentTool 与子代理执行体系

重点回答：

- agent 定义为什么不是硬编码在工具里，而是通过 agents 目录、内建 agent、plugin agent 和 CLI 覆盖层共同装配？
- getAgentDefinitionsWithOverrides、getActiveAgentsFromList、runAgent 分别处在子代理体系的哪一层？
- 子代理为什么既要继承父上下文，又要重新构造自己的 system prompt、tool pool、MCP clients、sidechain transcript？
- LocalAgentTask、后台 agent、前台 fork agent、in-process teammate 为什么能共用同一条 runAgent 主链？

### 第 16 章：MCP 连接、资源与技能工具接入专题

重点回答：

- services/mcp/client.ts 为什么才是 MCP 能力总线，而 MCPConnectionManager/useManageMCPConnections 只是交互层管理器？
- connectToServer、fetchToolsForClient、fetchCommandsForClient、fetchResourcesForClient、prefetchAllMcpResources 如何把 MCP server 变成运行时能力？
- ListMcpResourcesTool、ReadMcpResourceTool 为什么不是“附属工具”，而是 MCP 资源模型进入主 Agent 循环的关键桥梁？
- MCP skills、MCP commands、MCP tools、远端连接状态更新为什么要分层缓存、分层同步，而不能揉成一次性初始化？

### 第 17 章：Task 框架、Coordinator 与后台任务体系

重点回答：

- Task.ts、utils/task/framework.ts、LocalAgentTask、LocalMainSessionTask 分别处在后台任务体系的哪一层？
- 为什么 Claude Code 要把 agent、shell、workflow、remote agent 都收敛进统一 task state，而不是各自管理生命周期？
- retain、evictAfter、foregroundedTaskId、viewingAgentTaskId 如何把“后台执行”和“前台查看”拆成两套状态？
- CoordinatorTaskPanel、teammateViewHelpers、PromptInput 导航为什么说明任务系统已经直接进入 REPL 交互层？

### 第 18 章：Hook、Elicitation 与权限回调链

重点回答：

- frontmatter hooks、session hooks、executeSubagentStartHooks 在子代理和主会话里分别怎样注册和清理？
- MCP elicitation 为什么不是单纯的一个弹窗，而是一条 hook 可拦截、AppState 可排队、notification 可确认的交互链？
- interactiveHandler、bridge permission callbacks、channel permission relay、swarm permission request 如何在同一条 permission race 里竞争决议？
- 为什么这个工程把“权限批准”设计成多来源仲裁系统，而不是单一终端确认框？

### 第 19 章：Swarm 协作、InProcessTeammate 与邮箱协议

重点回答：

- InProcessTeammateTask 为什么不是普通子代理封装，而是一个真正进入 Task 框架的常驻协作节点？
- identity、pendingUserMessages、isIdle、shutdownRequested 这些状态字段揭示了怎样的 actor 化建模？
- teammate mailbox、useInboxPoller、permissionSync 如何把普通消息、权限审批、计划确认、关闭请求统一进一套控制协议？
- 为什么 swarm 的核心难点不是“多开 agent”，而是“让 leader UI 成为团队统一控制面”？

### 第 20 章：Remote Control、Bridge 与统一控制面

重点回答：

- useReplBridge、initReplBridge 为什么本质上是在把本地 REPL 变成一个可远程接入的会话控制器？
- useRemoteSession、useDirectConnect、useSSHSession 三种远程模式在 REPL 顶层为什么可以被压成同一种 activeRemote 接口？
- direct connect、bridge、remote viewer、ssh 这几条链路各自改变了哪一层 transport，又有哪些统一的审批与中断语义？
- 为什么说 Claude Code 的远程能力不是“屏幕共享”，而是一套统一的 session control plane？

## 五、建议的源码主线图

```mermaid
graph TD
    A[entrypoints/cli.tsx] --> B[main.tsx]
    B --> C[setup.ts]
    B --> D[commands.ts]
    B --> E[tools.ts]
    B --> F[context.ts]
    B --> G[replLauncher.tsx]
    G --> H[screens/REPL.tsx]
    H --> I[QueryEngine.ts]
    I --> J[query.ts]
    J --> K[services/tools/*]
    J --> L[services/api/*]
    E --> K
    D --> H
    F --> I
    E --> M[services/mcp/client.ts]
    M --> N[AgentTool/runAgent.ts]
    M --> O[MCPConnectionManager/useManageMCPConnections]
    J --> P[Task.ts / utils/task/framework.ts]
    P --> Q[CoordinatorAgentStatus.tsx]
    N --> R[utils/hooks.ts]
    P --> S[InProcessTeammateTask / useInboxPoller]
    S --> T[teammateMailbox / permissionSync]
    H --> U[useReplBridge / useRemoteSession / useDirectConnect / useSSHSession]
    U --> V[bridge / direct connect / ssh control plane]
```

## 六、阅读方法建议

这套工程不适合“按目录树平均阅读”，建议采用下面的方法：

1. 先找总线文件：main.tsx、QueryEngine.ts、query.ts、tools.ts、commands.ts、context.ts。
2. 每读到一个外部函数调用，就先判断它属于哪一层：启动层、会话层、循环层、工具层、扩展层。
3. 把“什么时候调用”和“为什么现在调用”写出来，而不是只记“它做了什么”。
4. 尤其关注源码注释，这个项目很多关键设计意图都直接写在注释里。

## 七、全书使用说明

这套教程不是 API 手册，而是“执行链路精读”。

所以每一章都会按下面的模板展开：

1. 模块职责
2. 执行链路
3. 关键源码片段
4. 设计动机
5. 与其他模块的关系
6. 阅读时容易忽略的点

接下来从第 1 章开始，先把整条启动总线梳理清楚。
